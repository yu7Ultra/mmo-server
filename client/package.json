{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:mock": "tsc -p tsconfig.mock.json",
    "run:mock": "node dist-mock/command/mockClient.js",
    "mock": "yarn build:mock && yarn run:mock"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "~5.8.3",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "@colyseus/schema": "^3.0.61",
    "colyseus.js": "^0.16.0",
    "express": "^5.1.0",
    "pixi.js": "^8.0.0"
  }
}
